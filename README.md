Example of fullstack development using Angular, Flask and MySQL.

To deploy the build, in a clear enviornment, run

```bash
docker-compose up -d
```

Structures

- webapp: Angular frontend
- static: Dist folder of ng build
- common: Supporting python library
